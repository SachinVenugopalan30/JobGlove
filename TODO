- Add company name and job title fields next to the job description
- Add user's name field
- Add an option to create a new resume from scratch, still using jake's resume template
- Add a resume review section first with the job description before tailoring the resume
- In the resume review section, highlight each bullet point with the strengths of the initial resume, and how to refine it
- After the AI has finished tailoring the resume and before rendering it on LaTeX, show the points in the resume so that the user can make tweaks where necessary, if necessary
- Add a section to preview and download resumes that have been tailored in the past, add a search field to quickly search by file name (remember to keep options to redownload both the pdf and tex files)
- Use \textbf{} to bolden numbers and metrics that quantify each bullet point in LaTeX
- Use this color scheme for the website - 000814, 001d3d, 003566, ffc300, ffd60a
- Use shadcn and tailwind for design, make it more interactive and with smooth animations everywhere
- Create a nice logo for the website along with the favicon
- File name will be <user's name>_<company>_<title>_resume.pdf/.tex
- Prepare to run everything within a docker container
- Prepare to receive inputs from a chrome/firefox extension that auto fills the input fields from various job sites